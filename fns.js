const fns = {
    sum,
    res,
    mul,
    div,
    pow,
};

function sum(a, b) {
    return a + b;
}

// Completar estas funciones

function res(a, b) {
    return b - a;
}

function mul(a, b) {
    return a * b;
}

function div(a, b) {}

function pow(a, b) {}

module.exports = fns;
